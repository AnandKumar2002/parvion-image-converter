export interface Category {
  id: string;
  title: string;
  slug: string;
  description: string;
  icon: string;
  color: string;
  hoverColor: string;
  bg: string;
  gradient: string;
  order: number;
  isActive: boolean;
}
